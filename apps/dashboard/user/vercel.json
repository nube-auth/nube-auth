{
	"buildCommand": "cd ../../../ && pnpm build --filter=@proofa/dashboard-user",
	"devCommand": "pnpm dev",
	"installCommand": "cd ../../../ && pnpm install",
	"framework": "vite",
	"outputDirectory": "dist",
	"rewrites": [
		{
			"source": "/api/:path*",
			"destination": "https://api.proofa.sh/:path*"
		},
		{
			"source": "/((?!assets/).*)",
			"destination": "/index.html"
		}
	]
}
